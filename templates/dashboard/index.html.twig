{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <div class="accordion" id="newsAccordion">
        {% for i, source in sources %}
            <h2 class="accordion-header" id="heading-{{ i }}">
                <button
                    class="accordion-button collapsed"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#collapse-{{ i }}"
                    aria-expanded="false"
                    aria-controls="collapse-{{ i }}"
                >
                    {{ source.name }}
                </button>
            </h2>
            <div 
                id="collapse-{{ i }}"
                class="accordion-collapse collapse"
                aria-labelledby="heading-{{ i }}"
                data-bs-parent="#newsAccordion"
            >
                <div class="accordion-body">
                    Lorem ipsum dolor sit amet, consectetur adipisicing elit. Iste maiores quasi voluptate numquam doloribus magni minima, vitae ad veniam, consequatur, nam ducimus libero repellendus architecto nobis cum maxime incidunt. Et!
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
