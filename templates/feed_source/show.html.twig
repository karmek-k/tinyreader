{% extends 'base.html.twig' %}

{% block title %}FeedSource{% endblock %}

{% block body %}
    <h1>FeedSource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ feed_source.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ feed_source.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ feed_source.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('feed_source_index') }}">back to list</a>

    <a href="{{ path('feed_source_edit', {'id': feed_source.id}) }}">edit</a>

    {{ include('feed_source/_delete_form.html.twig') }}
{% endblock %}
